

能調用 方法 的一定是 對象
文件 本身就是一個對象

對文件操作流程
    1.打開文件,得到文件句柄並賦值給一個變量
    2.通過句柄對文件進行操作
    3.關閉文件
    常用的三種模式'r','w','a'

'r'開放閱讀（默認）
'w'打開寫入，先截斷文件
'x'創建一個新文件並打開它進行寫入
'a'打開寫入，如果存在則附加到文件的末尾
'b'二進制模式
't'文字模式（默認）
'+'打開磁盤文件進行更新（讀寫）
'U'通用換行模式（已棄用）