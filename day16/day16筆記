需先撐握的知識點
1.作用域:L_E_G_B
2.高階函數:
    需先滿足2條件:
    a.函數名可以做為參數輸入
    b.函數名可以做為返回值

3.閉包定義:
    如果在一個內部函數裡,對在外部作用域(但不是在全局作用域)的變量進
    行引用,那麼內部函數就被認為是閉包(closure)

    閉包 = 函數塊(內部函數) + 定義函數時的環境


裝飾器(函數){需尊守編寫代碼開放封閉原則}
    封閉原則:不可對原代碼的修改避免造成重大出錯
    開放原則:在不修改原代碼下










